import test from 'ava'
// createHandlerHarness(handler) {
//   // setup a router
//
// }
//
//
// test.beforeEach((t) => {
//   const mf = new Miniflare({
//     // relative to wrangler.toml path
//     scriptPath: 'spec/authorizationHandler/index.js',
//     buildCommand: 'esbuild --bundle --sourcemap --outdir=dist ./spec/authorizationHandler/index.js',
//   })
// })
//
test("just give it a go", async ({ pass }) => {
  pass()
  // Get the Miniflare instance
  // const { mf } = t.context;
  // // Dispatch a fetch event to our worker
  // const res = await mf.dispatchFetch("http://localhost:8787/");
  // // Check the count is "1" as this is the first time we've been to this path
  // t.is(await res.text(), "authorization");
})
//
// const { mf } = t.context
